<CanvasWidget>:
    BoxLayout:
        orientation: 'vertical'
        padding: 2
        spacing: 2
        x:0
        top: root.top
        Button:
            text: 'Delete'
            on_release:root.clear_canvas()
        Button:
            text: 'Show'
            on_release:root.show_info()
        Button:
            text: 'lock'
            on_release:root.lock_draw()